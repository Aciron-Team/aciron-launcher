# --- Node / Vite ---
node_modules
dist
dist-ssr
*.local

# --- Rust / Tauri ---
src-tauri/target
src-tauri/gen

# --- Secrets / local config (не публикуем) ---
.env
.env.*
*.key
*.pem
secrets.json
**/apikeys.json
**/*.secret.*
# Токен прокси (ACIRON_PROXY_TOKEN) вшивается в бинарь при сборке.
.cargo/config.toml
**/.cargo/config.toml

# --- Личные / рабочие файлы ---
TODO.md
launcher-screenshot.png
/aciron-launcher

# --- Артефакты релиза (генерируются, не в репо) ---
latest.json

# --- Логи и временное ---
*.log
npm-debug.log*
.cache
design.md

# --- ОС / IDE ---
.DS_Store
Thumbs.db
.idea
.vscode
*.suo
*.user
